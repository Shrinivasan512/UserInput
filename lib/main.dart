import 'package:flutter/material.dart';
import 'package:userinput/App.dart';

/*  Create the main method  with the type as void   */
void main(List<String> args) {

  // Invoke runApp to attach the App widget as the root to start the application
  runApp(const App());
}